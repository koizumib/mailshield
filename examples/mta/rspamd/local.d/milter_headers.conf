# Authentication-Results ヘッダのみ付与する
# smtp-gateway はこのヘッダを読んで SPF/DKIM/DMARC/ARC の結果を取得する
use = ["authentication-results"];

# Docker ネットワーク（172.x.x.x）はローカル IP と判定されるため、
# skip_local を無効化してローカル IP からのメールにもヘッダーを付与する
skip_local = false;
skip_authenticated = false;
